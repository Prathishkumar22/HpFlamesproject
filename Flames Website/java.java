import java.io.*;
import java.util.*;
class Main{
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);
        System.out.println("Enter the string");
        String str=scan.nextLine();
       int count=0,len=0;
       {
        len=str.length;
        char[] name=str Array[i].tocharAt[i];
        for(int i=0;i<len;i++){
            if(name[0]==name[i]&&((name[0]>=65&&name[0]<=91)||))
        }
       }

    }
}